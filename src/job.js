import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Job = () => {
  return (
    <View>
      <Text>INi adlah halmaan job</Text>
    </View>
  );
};

export default Job;

const styles = StyleSheet.create({});
